# 交接：接手这个项目要先知道的事

写给下一个 session。按顺序读，前三节是必读。

---

## 1. 三十秒上手

```bash
cd ~/Projects/crystal-quest
python3 tools/serve.py 8123 --lan     # 开发服务器（--lan 让手机也能连）
```

| 要做什么 | 怎么做 |
|---|---|
| 本机玩 | `http://localhost:8123/` |
| 手机玩（同 Wi-Fi） | `http://<本机 IP>:8123/` |
| **线上（任何网络）** | **https://maxwu1981.github.io/crystal-quest/** |
| 跑测试 | 浏览器开 `http://localhost:8123/tests/`，看「88/88 通过」（数字会随新测试长） |
| 数值模拟 | `http://localhost:8123/tests/?balance` |
| 模块体检 | `python3 tools/lint_modules.py`（**三项都要过**） |
| 改完发布 | `bash tools/deploy.sh`（已登录过，不会再要授权） |
| 调试模式 | 网址加 `?debug`：分页标题显示地图 id 与坐标，手机上多出「血 / 战」两个钮 |

**没有 Node。** python3 是 3.9。要跑 JS 用 macOS 自带的
`/System/Library/Frameworks/JavaScriptCore.framework/Versions/A/Helpers/jsc -m`。

---

## 2. 用户是谁、他要什么

用户是**导演**，我是开发。他在两台电脑上开 Claude，手机上试玩。

**他反复强调过、绝不能忘的三件事：**

1. **不要一直告诉他怎么做，直接执行。** 他明说过「请执行啊不要我一直问」。
   凡是我能做的就做完再报，只有真正需要他拍板的才问（而且要给选择题 + 附图）。
2. **画面一律用 Gemini 生成，不要自己程序化画。** 原话「不管是迷宫的元素、
   还是地图的背景、还是战斗的背景，都请用 Gemini 来生成会比较美观」。
   例外是逐帧动画（魔法特效、召唤演出）——那是按时间画的，不是静态图。
3. **画面往《歧路旅人》HD-2D 靠。** 见 `docs/HD2D方案.md` / `HD2D评估.md`。

他最近下了一份**总监团队**的角色设定，列出四类硬性规定（全屏 UI、脱战清 Debuff、
自动补血、道路与特效分离）。前三条已落地，第四条还没做。

4. **改完要自己进游戏确认，并且要发布出去。** 2026-09-07 他为这条专门骂过一次：
   全屏改完、测试全绿、我在 localhost 上量到四边黑边都是 0，而他手机上一点变化都没有——
   因为三个提交**全躺在本地，一次都没 deploy**。他玩的是线上那份。
   完整的四步验收写进 CLAUDE.md 了（「验收：什么叫改好了」那一节），照着做。

---

## 3. 交接单原本列的三件事——**都做完了**（2026-09-08）

下面三节留着不删：里面记的是「查出来的根因」和「量的方法」，
其中两条的根因跟原本写的不一样，那才是真正值钱的部分。
接下来该做什么见第 7 节（八元素迷宫已全部落地、走地图 HD-2D 已落地并量过、
交通载具做了一半——细节与最新状态见第 7 节表格，不要只看这两句话）
与第 8 节（等导演拍板的事）。

### ~~① 三个物理职业一个技能都没有~~ ← 已做（2026-09-07），下面留着当验收单

三个物理职业各有三招，1 / 4 / 7 级各开一招，`data/skills.json` + 「战技」指令。
schema 与设计理由写进 CLAUDE.md 了，这里只留导演要拍板的两点：

**门槛选的是「冷却回合 + 少数几招收 HP」，没有加第二套货币。**
冷却存在战斗 actor 上，战斗结束就丢，进不了存档；HP 只有拼命（25%）与七星步（10%）收。
另一条路（限次）没选，理由是它在一场三回合的杂鱼战里等于无限。**这条随时可以换。**

| 职业 | 1 级 | 4 级 | 7 级 |
|---|---|---|---|
| 拳头师 | 连环拳（两段，缓 1） | **碎甲**（无视六成防御 + 破甲） | 拼命（放 25% 血，必中重击） |
| 家将 | **开脸**（挡煞：敌人先打他 + 防护） | 踏罡（全体） | **七星步**（八段） |
| 山猎人 | 屏息（必中 + 高会心） | 抹毒（附带中毒） | 扫山（全体） |

导演点名的破甲 / 嘲讽 / 多段斩三样都在里面（粗体那三招）。
顺带修了两件事：`statusPhase` 的状态倒计时改成照 `STATUS.turns` 泛化（原本是手写名单）；
自动战斗那边有一句无差别的「普攻反正打得死一只就别用战技」，会让家将从头到尾一招都不放。

**这一条也写错了，改一下**：原话说「战技没有自己的演出，借用普攻刀光」，
但 `src/battle/skillFx.js`（272 行）九招早就各有一套独立演出（`fist2`/`sunder`/`allout`/
`openface`/`stomp`/`sevenstar`/`aim`/`venom`/`sweep`），`effects.js` 也确实展开了
`SKILL_FX`，`tests/?`「战技：九个演出真的画得出来，时长在 0.3–0.9 秒之间」一直是绿的。
和第 7 节 HD-2D 走地图那次是同一类坑：**不要照抄这份文档的「状态」栏，先去代码里核一遍**。

### ~~② 召唤的 MP 门槛太高~~ ← 已做（2026-09-08）

MP 22–60 → 10–28，自动战斗补上了整条請神分支。细节写进 CLAUDE.md 的 summons 那节。
两个值得记住的点：

**「一场只请得动一尊」有一半不是 MP 的锅，是吕布。** 他 `drain:'mp'` 放完 MP 归零，
而论单发伤害他永远排第一，所以 AI 每场第一手请他然后整场结束——实测 20/20 场都是他。
`pickSummon` 改成按「有效伤害 ÷ **真实代价**」排（drain 那位的真实代价是身上全部 MP）之后
才真的能请一轮。**只调数值不动这条，改了也白改。**

**`tests/balance.js` 现在有童乩路线与「请神」一栏。** 之前那张表里根本没人请过神，
改 summons.json 的数字前后跑出来一模一样，看着像「改动没影响」。
改召唤的数值前后一定要对这一栏（现在 Boss 战 1.3–1.9 尊，改之前是 0.95）。

### ~~③ 道路与特效分离~~ ← 已做（2026-09-08），但**根因不是交接单原本写的那个**

交接单原话：「我生成的物件瓦片每一张都自带一层地面，拼在一起就是几种地板打架」，
并说做这件事的 agent 中断了、一行没写。**两句都不对**——量过之后：

- 十张物件瓦片**早就是透明底了**（实心 24–41%、外圈实心 **0%**），一层地面都没自带；
- 合成管线也早就写好了（`terrain.js` 的 `OBJECT_GROUND` / `SEAMABLE`，
  按邻居决定铺哪种地，`seamTile` 还顺手烘了接地影）。

真正的根因是另一件事，量出来才看得见：**`stone_wall` 漏在 `CASTER` 表外**。
隘寮石城整整 351 格是它，而它和自家地板 `flagstone` 的明度差只有 **10.9/255**
（墙 85,92,94 / 地 105,101,97，ΔRGB 32/765）——本来就快分不出来了，
再加上漏登记 ＝ 墙脚一道影子都没有，整张图就读不出哪里是路。
（其余几张没这个问题：洞窟墙地差 27、古塚 67、室内 53。）
而且 `inert` 也是拿 `CASTER` 判的，所以漏登记的墙**还反过来接受地面那一套**：
崖影投到墙上、青苔裂缝长到墙面上。

补上 `stone_wall: 1` 与 `ore_vein: 1` 之后，实机量到墙脚的明暗落差 **13.6 → 37.4**。
`tests/cases/maps.js` 加了一条：每一种「挡路」的瓦片都必须登记成投影源，
或者明确属于三类例外之一（水/发光石、抠底合成那批、房子那套）。拿探针验过真会红。

**教训**：交接单上写的「根因」也要自己量一遍。这条如果照着做，
就是花一整轮 Gemini 去重出十张本来就没问题的瓦片。

---

## 4. 出图管线（Gemini）——这条最容易踩坑

**用内置浏览器 `mcp__Claude_Browser__*`，不要用 Claude in Chrome。**
用户同时在两台电脑上开 Claude，Chrome 会互相抢同一个 Gemini 会话。

开工顺序（**每一步都别跳**）：
1. **先刷新页面**。闲置超过一分钟就会出问题。
2. `eval(localStorage.getItem('cq'))` 恢复辅助函数（`__fill` / `__go` / `__ready` /
   `__imgs` / `__tile` / `__sheet` / `__grid` / `__shipStrip` / `__save`）。
3. 点两个开关：「上传和工具」→「制作图片」，再把「宽高比」设成 1:1。
   **浮层菜单在浏览器窗格隐藏时不会渲染**（Angular 的 `.cdk-overlay-pane` 要动画帧），
   点不动就先 `tabs_select` 把分页顶到前面；菜单项要从 `.cdk-overlay-pane` 里按文本找。
4. **卡住（一直显示 Creating your image）就刷新。**
   不要去猜额度或审核——我猜错过两次，绕去改写提示词、另开对话，全是白费。
   （唯一真的被内容挡下的是「石棺」：带 tomb / chest / lid 的提示词两次都被
   Gemini 自己标题成 Error 且回应为空，换成中性说法就出来了。）
5. 回传：`location.href='http://127.0.0.1:8124/px?m=1&f=<名字>.png&d='+encodeURIComponent(base64)`
   接收端是 `tools/art_receiver.py`（要先跑起来）。
   **文件名只能是字母数字下划线**，连字符会被拒。多张要拼成一条长图一次传
   （整屏 base64 会超过 URL 长度上限）。

---

## 5. 这个项目栽过的坑（每一条都真的发生过）

- **对象字面量后写的键会静默盖掉 `...展开`。** `effects.js` 曾经写成
  `{ ...SPELL_FX, …, fire, thunder }`，于是新写的多段式魔法演出**一次都没播过**，
  而测试全绿、控制台干净。`lint_modules.py` 的 C 项专查这个。
- **`replace()` 找不到目标会静默不改。** 我给 `render.js` 插落影函数时，
  锚点 `const GHOST = 0.42;` 早就被删了，结果只插了调用点、函数本体没进去。
  **每次 python replace 之后都要 grep 验证。**
- **模板串里的 CSS 写 `\21BB` 会被当成八进制转义**，整个模块 SyntaxError、游戏白屏，
  而测试和 lint 都看不见。测试「每个模块都 import 得动」就是为这个加的。
- **遇敌区没在 `ZONE_BG`/`MAP_BG` 登记 → 战斗背景静默退回通用**，
  地平线停在旧位置、人浮在天上，而地图、遇敌表、测试全都正常。
  隘寮石城与万金古塚就这么漏了一整轮。
- **`posterize()` 三通道独立取整会把近灰的东西染出色相**——(120,125,135) → (109,109,146)，
  整面城墙变蓝紫。要用 `posterize_luma`（只压明度）或 `snap_to_palette`。
  但**`posterize_luma` 也有缺陷**：它 r,g,b 同乘一个系数，会把彩度 max−min 一起放大
  （评估文档量过）。物件瓦片一律走 `snap_to_palette`。
- **`set_art.py` 会把非角色非怪物的母版当瓦片压成 16×16 逻辑。**
  背景图必须走 `is_bg()` 排除——当初 16 只怪就是这么全被压成方块的。
- **浏览器窗格隐藏时**：截图超时、Chrome 把定时器节流到 1 秒一次（等图的循环会卡死）、
  Angular 浮层不渲染。要看画面就把画布渲染一帧后回传本机：
  ```js
  const g=window.game; g.render(0,{fps:60});
  const cv=document.createElement('canvas'); cv.width=768; cv.height=672;
  const c=cv.getContext('2d'); c.imageSmoothingEnabled=false;
  c.drawImage(g.canvas,0,0,g.canvas.width,g.canvas.height,0,0,768,672);
  await fetch('http://127.0.0.1:8124/px?f=SHOT_x.png&d='+encodeURIComponent(cv.toDataURL('image/png').split(',')[1]));
  ```
  落在 `assets/art/`，**看完删掉**。
- **连续 `getImageData` 会让 Chrome 把画布降级成软件光栅**，帧耗时 2ms→60ms 且不会恢复。
  量性能要重开页面。
- **`git add -A` 会把正在跑的 agent 的半成品一起提交。** 派 agent 之前先划清文件归属，
  提交时只 add 自己动过的路径。

---

## 6. Agent 使用要点

**授权已经搬进 CLAUDE.md 了**（「复杂任务一律派多个 agent 并行」那一节）。
2026-09-08 导演问「为什么你不会自动开不同的 agent」——根因是 Claude 的默认指令写着
「除非用户、**CLAUDE.md**、或 skill 明确要求，否则不要用 Agent」，
而这条要求当时只写在**本档**里，不是 CLAUDE.md，所以每个新 session 都读成
背景说明而不是授权。下面这些经验留着，规矩本身看 CLAUDE.md。

这个 session 里 **16 个 agent 有 6 个撞会话额度中断**，
其中 4 个零产出。经验：

- **每个 agent 独占一批文件**，任务描述里把「绝对不要碰」列清楚。
  几个 agent 同时改一个档一定互相盖掉。
- **浏览器分页也要分**：让每个 agent `tabs_create` 开自己的，
  否则它们会把彼此的分页导航走。
- 中断的 agent 常常已经做完大半（成果留在工作区），**先看 `git status` 再决定重派还是接手**。
- 额度重置时间会在错误信息里写（这次是 22:30 Asia/Saigon）。

---

## 7. 已有的设计文档

| 文档 | 内容 | 状态 |
|---|---|---|
| `主线设计.md` | 六幕、六鎮物、光之水晶、地心的老地基 | 只有第一幕能玩 |
| `八元素迷宫.md` | 八座元素迷宫的地理、原型、机制 | **六座支线全部接线上线**（2026-09-08） |
| `交通与世界移动.md` | 牛車→五分車→竹排→火輪四级载具 | **第 0/1 步（地基＋牛車）已落地**，2/3/4 步（五分車/竹排/火輪）还没做 |
| `20小时内容结构.md` | 时长构成、12–14 vs 20 小时之争 | **写完了，没落地** |
| `HD2D方案.md` / `HD2D评估.md` | 风格路线与独立评估 | 部分落地 |
| `HD2D走地图.md` | 走地图的 HD-2D 化 | **已落地并量过**（见下） |
| `职业与召唤.md` | 三种等级、转职承接、请神 | 已落地 |
| `手机上玩.md` | 两条部署路线 | 已落地 |

**这一条曾经写错，改一下别再传下去**：早前这里写「走地图那半的 HD-2D 完全没做」，
那句不对——`lightmap.js`/`postfx.js` 当时就已经接进 `FieldScene` 了，只是没人量过效果。
2026-09-08 量了一遍，发现受光棱与立面暗部是好的，但长投影的斜坡锚点错了，
矮物件（树/木箱/床）的影子整条压在自己格子里被擦掉，六堆平原 164 棵树零投影。
修了那个锚点（`docs/HD2D走地图.md` 第三节），三张验收图的「建筑北侧比南侧暗」的读数
从 14.3/9.9/7.3 提到 22.7/15.5/13.2。同一天顺手把这份文档自己留的两个「明确不做」
补掉了：角色/NPC 的落影从居中圆改成了朝光向偏移+拉长+外晕，粒子景深也接上了
（远的透明度打折、画成 2×1）——都在 `src/field/npc.js` / `ambience.js`，跟 `lightmap.js`
用同一个光向常量（`SUN`），没有各画各的。
**这件事本身也是一条教训**：不要照抄这份交接单里「状态」那一栏就动手，
先去代码里核一遍、量一遍——这份文档自己就在同一节里栽过一次这个坑。

---

## 7.6 八元素迷宫剩下六座：~~规格出了，但六座全部判 needs-work~~ ← **六座全部接线上线**（2026-09-08）

派了一个 12-agent 的 Workflow（六座各「出规格 → 对抗验证」一轮）。
**没有一座能直接照做**——共同病灶是难度算错参照系（把「20 级迷宫」接成
「随时进得去」）、宝箱收益是现有全部宝箱总和的数倍、`tests/balance.js` 结构上
看不到饰品槽。逐座的具体卡点、预估修复工时（3–6h/座）、修法全部记在
`docs/八元素迷宫_剩余六座_核验记录.md`——那份现在是**完工记录**，六座
（燈/風口/出火地縫/木馬道/空伙房/塭岸）逐一接线验收的过程与最终改动都在里面。

**没有一份规格是照单全收的**：接线前都独立重跑过它的空间/数值结论，
六座里至少有两座（空伙房、塭岸）连「验证」给的修法本身都核出了错——
细节见 `docs/八元素迷宫_剩余六座_核验记录.md` 各自章节，不要重新调查。

## 7.5 排队等做的（导演点名，按加入顺序）

### ~~① 村民的角色图与走路动作（2026-09-08 导演提出，不急）~~ ← **已完成**（2026-09-08）

两个问题，导演的原话：
- **「村民的人物比例跟我们设计的角色比例不同」** —— 七种村民 NPC
  （man / woman / elder / kid / merchant / innkeeper / guard）跟六个可玩角色不是一套比例。
  根因是 `tools/char_prompts.json` 里村民的 `build` 字段当初写的是「ordinary adult build」，
  「adult」这个词把模型拉向写实解剖比例，四五头身站在三头身的队伍里明显矮一截、头小一圈——
  这条 `_villagers` 栏当时已经记录了修法（改成「chunky sturdy build, same big-headed
  proportions as the hero party」），但一直没有照着重新出图。这次全部重出：
  七种村民各一张 768×192 长图（`__sheet()` 生成，`tools/split_strip.py` 切成 6 张），
  实心像素比对齐可玩角色的量级（改前 4797–6886，改后 5273–7203，量级参考同栏可玩角色 4936–7699）。
- **「走路的时候往左往右往上往下看不出感觉，只是一个图片在移动」** —— 七种村民各补齐
  down / down_walk / left / left_walk / up / up_walk 六帧（原本各只有 `down` 一张）。

出图管线踩到的新坑（补进第 4/5 节的教训清单）：
- **`window.__sprite()` 走浏览器 `javascript_exec` 文本回传时，大到一定程度会在本地拼接阶段
  被截断**（46068 字符那次没触发工具自带的溢出转存、但内容缺了一大截，肉眼看输出图直接漏了下半张）。
  修法：回传前一律拼接一段几万字符的填充串（`JSON.stringify({b64, pad: 'x'.repeat(60000)})`），
  强制触发工具的「超限转存到文件」路径，再从那个文件用 Python 解析——转存路径这条会全量落盘，
  不会丢字节，比直接吃 `javascript_exec` 的文本返回值可靠。
- **`tools/proportion_check.py --sheet` 导出的对照图曾经是坏的**：`make_sheet()` 硬编码
  按 ART=2 时代的 32×48 源图做 4 倍最近邻放大取样（`y // 4`），ART 早已切到 6
  （成品图 96×144），于是每张只读得到源图左上角一小块，对照图变成「只看得到头」。
  已修成按源图实际尺寸等比例映射（`y * h // CH`），现在任何 ART 下都能出完整对照图。
- Gemini 出图偶发「生成失败、标题带 Error/Failure、回应是空的」，跟内容审核无关
  （`elder`、`guard` 两次都撞上，提示词本身平平无奇）——刷新页面用同一份提示词原样重发一次就好，
  不要瞎猜是哪个词被挡然后去改写提示词。

## 8. 等导演拍板的事

**下面六条，2026-09-08 已经拍板五条**——这一节原本是全新问题清单，现在留着当决定记录，
不要看到旧措辞又当成没做过重新调查一遍。

0. ~~战技的门槛用「冷却 + 少量 HP」对不对~~ ← **维持现状**。拼命 25% 最大 HP 换一次
   必中重击、缓 4 回合，就是最终版本，没有改得更硬派或更爽快。
1. ~~那 6 张生成好的迷宫接不接~~ ← **已接**。`fort_ailiao`/`tomb_wanjin`/
   `altar_1..3`/`furnace_1..2` 全部在 `config.json` 里、全部有 warp 能走到
   （`altar_1..3` 经 `bogong.json` 的伯公坛进去）。
2. ~~23 件 tier 6–10 装备定义了却永远拿不到~~ ← **已补完**。`tests/cases/gear.js`
   那条「每件神话装备都真的拿得到」现在是绿的，六座八元素迷宫（见第 7.6 节）
   接线时又把 tier 6/7 那批饰品/消耗品出处补得更满。
3. ~~难度：全开箱 4 级就 100%~~ ← **维持现状，不再调整**。根因先查错过一次——
   不是 `def ≥ 60` 单挑免疫，是「全开箱」这个测试档位 2026-09-08 才被补全
   （四个人三格装备全换神装，等于 100% 全收集），草薙剑 72 攻在 3~4 回合内
   打完乌火，防御高低根本来不及起作用。试过把乌火 HP 1250→1650、暴击
   10→28 一起调，全开箱 4~8 级**照样 100%**，反而把纯商店路线 12 级从
   87% 打到 60%、8 级打到只剩 3%——纯调 Boss 数值追不上全开箱的输出，
   还会先伤到本来就该难的路线。改动已撤回，乌火数值维持原样。
4. **海底地图要不要做——已拍板方向，还没排上施工**：不做真潜水/海底地图
   （`交通与世界移动.md` 的立场），改做「退潮沙洲＋沉船的」。这个还没做，
   而且跟交通载具的进度绑在一起——`交通与世界移动.md` 把「外海的沙洲，
   退潮才有」列成竹排/火輪才到得了的地方，本档 7 节的载具目前只做到
   第 1 步（牛車），竹排/火輪（第 2/3 步）还没做，「沉船的」那只怪
   `data/enemies.json` 里也还没有。建议顺序：先把竹排接完，
   沙洲＋沉船内容跟着竹排那一步一起落地，不要在载具之前单独插队做。
5. ~~童乩腰上那三张汉字符箓——已拍板方向：整个西幻化~~ ← **已完成**。
   6 张母版（站姿+行走 × 下/上/左，右由左镜像）全部经 Gemini 重出，
   三张纸符换成三个铜铃铛，prompt 模板（`window.__CH.tangki`）已同步改掉，
   之后再出这个职业的图会自动带新设定。`char_tangki_down`（站姿正面）
   出图时脚被连通域抠图裁掉过一次——`tests/?` 的「所有角色精灵一样高」
   那条测过才发现（78% vs 其他 93~97%），重出才过。已跑 `set_art.py --force`
   派生正式美术、96 条测试全绿、部署上线并在 GitHub Pages 活链上重新
   确认过四个朝向的实机渲染。

开发看板（会持续更新）：https://claude.ai/code/artifact/431b5a92-66f8-49d4-9a0a-b7161775ac08
