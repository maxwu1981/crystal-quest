# 走地图那半的 HD-2D：方案与实测

2026-09-07。战斗那半已经做过一轮（透视斜面、落影、景深、尺寸随深度、Gemini 天空）。
这份文档只管**走地图**：内埔庄、六堆平原、罗经圈两层、隘寮石城、万金古塚。

先把 `docs/HD2D评估.md` 那句结论抄在最上面，因为下面每一条的排序都是从它推出来的：

> 看不出歧路旅人是因为**画面里没有光、人没有影、也没有一个统一的镜头**——
> 这三样都是二三十行代码，而力气花在了压色数上，那恰恰是唯一测不出观感变化的一项。

---

## 零、先看现状：走地图现在到底缺什么

六张图各渲一帧回读（`tools/_cap/B_*.png`，看完删）。逐张记下来的：

| 图 | 现状 | 最刺眼的一条 |
|---|---|---|
| 内埔庄 | 屋顶/墙有手画的明暗序，地上有**手摆的**影格（`shadow_dirt`/`shadow_grass`） | 影子**只在房子正下方一格**，房子右侧一点影都没有；草地在尖叫 |
| 六堆平原 | 山、林、草各自成片 | 山脚下的草是**全亮的**——一座山压在草地上却不投影 |
| 罗经圈外围/深处 | 有暗角（`vig`），靠主角照明 | 岩壁与地面**同一个亮度分布**，墙不像墙，像地面换了个花纹 |
| 隘寮石城 | 有 22% 的暖色调层 | 八根柱子、四面墙、两座台——**没有一片影子**，整张图是印在纸上的 |
| 万金古塚 | 最暗的一张 | 同上，且走廊两侧的墙没有任何受光边 |

三句话总结：

1. **光**：只有 `ambience.js` 一层**全屏乘法色调**（`tint`/`tintA`）+ 一个跟着主角走的暗角。
   没有方向。`src/assets/terrain.js` 的 `shadowTile` 压暗 N/W/NW 三个方向（说明**光向已经定死是左上**），
   但**没有对称的受光边**——有影无光，等于只做了减法。
2. **影**：`npc.js` 的 `drawShadow` 只有主角/NPC，树和宝箱各自在瓦片里烘了一小片。
   **墙、柱、建筑、山这些有体积的东西，一片投影都没有。**
   现有的崖影只有 `u(1)×5 = 5 逻辑像素`高——那是接触影（AO），不是太阳影。
3. **镜头**：战斗是压低的斜视（`perspRows`/`converge` 画出了灭点），走地图是正俯视。
   **同一个世界两套镜头**，切过去一眼就穿帮。

---

## 一、四件事的优先级与依据

排序依据只有一条：**「一眼能不能看出来」÷「改动量 × 风险」**。

### 第 1 位 — 光与影（合成一件事做）

**依据**：评估文档把「没有光、没有影」排在前两位，而这两样在走地图这边是**同一个缺口的两面**——
`terrain.js` 已经把光向定死在左上（`shadowTile` 只认 N/W/NW），缺的是
① 对称的受光边、② 长度像样的投影、③ 墙体的立面暗部。
三样共用同一个光向、同一次遍历、同一张贴图，**做一件事拿三样**，所以排第一。

不做的话，后面三件都是在一张平的图上加特效。

### 第 2 位 — 镜头（纵向光度渐变 + 移轴景深）

**依据**：这是唯一一件能让「走地图」和「战斗」看起来是同一台摄影机拍的。
而且它是**屏幕空间**的，跟地图内容完全无关：改坏了删两行就回滚，不可能弄脏任何一张瓦片。
排第二而不是第一，是因为在一张没有光影的平图上做移轴，糊掉的还是那张平图（这一点和
`docs/HD2D方案.md` 阶段 0 把景深往后放是同一个理由）。

### 第 3 位 — 落影升级与粒子景深

**依据**：现在的落影是 `alpha 0.22` 的**居中**椭圆——它是「贴纸的锚点」，不是「光压出来的影」。
改成朝光向偏移、拉长、并给站着的物件（柱/箱/树）也补一片，是十几行。
粒子景深（`par` 小的更淡更糊）是 `docs/HD2D方案.md` 第二节⑧记的零成本项，顺手做掉。

### 第 4 位 — 高处物件的 y 位移（评估后**决定不做**，见第五节）

---

## 二、逐条设计

### 2.1 光照贴图 `src/field/lightmap.js`（新档）

**为什么不改 `src/assets/terrain.js` / `terrainBake.js`**（`docs/HD2D方案.md` 阶段 0-3 原本是这么写的）：
`src/assets/**` 这一轮被另一位占着（在重出瓦片、改 `art.js`/`tiles.js`），碰不得。
换个地方做反而更好：光照是**地图级**的东西（一堵墙的影子要跨好几格），
而 `terrainBake.js` 那套是**一格一张缓存图**，跨格的长影子在那个模型里根本表达不出来
（现在的崖影只有 5 逻辑像素高，正是被这条边界卡住的）。

**做法：整张地图烘一张「光照贴图」，每帧一次 `drawImage`。**

- 分辨率 **1 贴图像素 = 1 逻辑像素**（`map.w*16 × map.h*16`）。最大的六堆平原 640×512 = 1.25 MB，
  内埔庄 1.20 MB，其余都在 0.7 MB 以下。渲染时被 ART=6 的变换放大 6 倍并开插值，
  于是**放大本身就是柔化**——影子的半影是免费的。
- 两张贴图，两种合成：
  - **阴影贴图**：不透明白底（白 = 不压暗），用 `darken` 画灰。渲染时 `multiply`。
  - **受光贴图**：不透明黑底（黑 = 不加亮），用 `lighten` 画光色。渲染时 `lighter`。

  两种合成模式**这个项目都已经在用**（`FieldScene` 的崖影层用 `multiply`，宝箱微光用 `lighter`），
  不引入没验证过的 `overlay` / `soft-light`——那两个万一在某个浏览器上退化成 `source-over`，
  会把整张贴图**盖在画面上**，是灾难性失败而不是降级。

**`darken` / `lighten` 在不透明画布上就是逐通道 min / max**，这一点是整套烘焙的地基：
同一堵墙的投影要画成七片依次偏移的方块，`source-over` 会让它们互相叠加成一团黑，
而 `darken` 取最暗的那一片＝最近的那一片，得到的正好是单调递减的拖影。

**四样东西画在这两张贴图上：**

1. **长投影**。光向沿用 `tiles.js`/`terrain.js` 的约定（左上来光），影子朝右下拖：
   `SUN = { dx: 0.55, dy: 1.0, len: 11 }`——一格高的东西拖 11 逻辑像素（约 0.7 格）。
   长度按物件高度缩放（`TALL` 表：整墙 1.0、屋脊 1.25、树 0.75、柱 0.8、木箱 0.4、床 0.25）。
2. **投影不落在墙上**。画完之后用 `lighten` 把所有「高物」格子擦回白色——
   否则村庄的屋脊会把影子投在自家屋顶上。村庄那 46 格**手画的影格**
   （`shadow_dirt`/`shadow_grass`）擦掉七成：那里本来就是影子，再乘一次会糊成一团黑，
   但留三成让新的软影**接着它往下化开**（现在那条边是硬的）。
3. **立面暗部**。高物格子的下缘 5 逻辑像素压一道由浅入深的暗带——
   墙就从「一块顶面」变成「一块顶面 + 一道转折」。这是导演说的「画出正面」里
   **唯一不动地图数据、不吃掉可走地面**的那半（另一半为什么不做见第五节）。
   它画在模糊之后，所以那道转折是硬的（立面的转折本来就该是硬的），而投影是软的。
4. **受光棱**。高物格子朝北（顶）的那条边描 3 像素亮线，朝西（左）的描 2 像素、亮度减半——
   `terrain.js` 只做了减法（崖影），这是补上的那一半加法。

**烘焙顺序**（顺序本身是设计的一部分）：
`投影 → 擦掉高物 → 降采样 1/3 再放大回来（软化）→ 立面暗部（硬）`。
模糊那一步用降采样，**不用 `ctx.filter='blur'`**——`docs/HD2D方案.md` 第五节第 3 条实测
小半径反而比大半径贵好几倍。而且这里是**烘焙期**跑一次，逐帧零成本。

**跳过被包住的墙**：四周（右/下/右下）都是高物的格子，它的影子会被第 2 步全擦掉，
直接不画。罗经圈那种整片岩壁的图，这一条把要画的格子砍掉七成。

**缓存**：按地图 id 存，只留最近 3 张（约 2~4 MB）。手机上不留 18 张。

**防闪**：整张贴图与时间**完全无关**，逐帧只是 `drawImage` 同一张图。**不可能闪。**

### 2.2 镜头 `src/field/postfx.js`（新档）

两件事，都在屏幕空间：

**（a）纵向光度渐变（空气透视）。** 一次带线性渐变的 `fillRect`（`multiply`）：
上缘偏冷偏暗（`rgba(150,168,196,0.30)`）→ 42% 处透明 → 下缘微暖（`rgba(255,236,206,0.14)`）。
俯视图里「上＝远」，远处压暗降饱和、近处提暖，是压低机位最便宜的读法。
战斗那边 `perspRows` 是把地面画成斜面，这边没有斜面可画，但**明度梯度是同一句话**。

**（b）移轴景深。** 整幅画降采样到 1/4 离屏，再把上 19% / 下 12% 两条带放大回来。
每条带切三小条、`globalAlpha` 依次 1.0 / 0.62 / 0.28 —— 相当于「清晰版」与「模糊版」
在内缘做交叉淡入，边界不会切出一条线。

**三条硬约束：**
- **必须画在 UI 之前**：地图名条、小地图、摊开的全图、对话框都在 `FieldScene.render` 最后几行。
  糊掉文字就是 bug。
- **不能糊到主角**：主角永远在镜头中心附近，两条带各自离屏幕中线还有 60+ 逻辑像素。
  室内小图（`mh <= H`，地图比屏幕矮）**整个关掉**——那种图主角可能站在任何高度。
- `game.state.settings.dof === false` 时关掉，给设置菜单留口（菜单在 `src/menu/`，这一轮不碰）。

**防闪**：同样与时间无关。

### 2.3 落影与粒子 `src/field/npc.js` / `src/field/ambience.js`

- `drawShadow` 加两个可选参数（高度、宽度系数），椭圆中心朝光向偏 1.5 逻辑像素、
  长轴拉到 0.34 格、加一圈更淡的外晕（两次 `ellipse`，第二次 alpha 减半、半径 ×1.35）。
  两个参数都有默认值，**所有既有调用点一个字不用改**。
- 粒子景深：`par`（视差）小的＝远的，透明度乘 `0.45 + 0.55*par`，并且远的画 2×1 而不是 1×1。
  `makeMotes` 已经有 `par` 字段，只是没人用它调亮度（`docs/HD2D方案.md` 第二节⑧记着这条）。

### 2.4 MOOD 补齐

`village` / `overworld` 是**唯二**没有 `vig` 的图，于是它们四个角是齐亮的，
读起来像一张摊平的贴图。补一层极浅的暗角（`a: 0.16~0.18`，比洞窟浅一半以上）。
另外给每张图加一个可选的 `sun: { a, lit, face, col }`，让光照贴图按场景调
（正午的村庄影子最实、洞窟里影子还在但受光棱偏冷偏弱）。

---

## 三、第二轮：三样里有两样成立，长投影**量出来几乎等于没有**

2026-09-08。上面那份方案写完后落地了，`lightmap.js` / `postfx.js` 两个新档都在跑，
`FieldScene` 也接上了 `drawLit` / `drawShade`。这一节是**接手之后先量一遍**的结果——
交接单说「走地图 HD-2D 完全没做」，那句是错的（这个项目已经栽过一次「照着交接单写的根因去做」，
见 `docs/交接.md` 第 3 节 ③）；但真去量，会发现**三样效果里只有两样立得住**。

### 3.1 量法

`window.__LIT` / `window.__SHADE` 两个开关（`lightmap.js` 底部的 `on()`）就是为 A/B 留的：
同一帧渲两次，一次两个都关（＝改前），一次都开（＝改后），各读回一次画面，
在**同样的格子集合**上取平均。不靠眼睛看截图。

三组取样，全部在渲染完成的画面上（不是在贴图上）量：

- **N / S**：屏幕内每一格「可走地面」，按它的邻居分成「建筑北侧」（南邻是高物）
  与「建筑南侧」（北邻是高物）两组，各取格子中段 8×8 逻辑像素的平均明度。
  光从左上来，**南侧该比北侧暗**，两者之差就是方向光的读数。
- **rim**：高物格顶上 3 行的平均明度 —— 受光棱。
- **face**：高物格「底部 4 行」对「中段 4 行」的明度差 —— 立面暗部。

### 3.2 改前的读数（三张验收图）

| 地图 | N−S（关） | N−S（开） | rim（关→开） | face（关→开） |
|---|---|---|---|---|
| 内埔庄 `village` | 7.3 | **14.3** | 64.7 → 83.0（+18.3） | 37.5 → 46.9（+9.4） |
| 六堆平原 `overworld` | 0.4 | **9.9** | 80.8 → 97.2（+16.4） | 9.4 → 17.8（+8.4） |
| 隘寮石城 `fort_ailiao` | 0.8 | **7.3** | 84.8 → 98.2（+13.4） | 3.5 → 17.2（+13.7） |

**受光棱与立面暗部是成立的**：隘寮石城的墙脚从 1.3 拉到 13.7，那正是「墙不像墙、
像地面换了个花纹」那条被治好了。

**长投影不成立**。直接量阴影贴图本身（不经过渲染），村庄地面**平均只压暗 7.2/255（2.8%）**，
六堆平原 8.4、隘寮石城 13.3。同一组截图关掉再打开，**肉眼分不出来**。

### 3.3 根因：斜坡的峰值全落在投影体自己身上，被下一步擦掉了

`bake()` 的顺序是「画投影 → 用 `lighten` 把所有高物格擦回白 → 降采样软化 → 立面」。
投影那一步是这么写的：

```js
const L = Math.min(hgt[i], s.cap) * SUN.len;   // 影子总长
for (let k = 0; k < K; k++) {
  const u = (k + 1) / K;
  fillStyle = gray(s.a * min(1,t) * (1 - k/K) ** 1.25);      // ← 斜坡从 k=0 起算
  fillRect(x*TILE + SUN.dx*L*u, y*TILE + SUN.dy*L*u, TILE, TILE);
}
```

`k = 0` 那一片（alpha ＝ 满值 `s.a`）的偏移量是 `L/K`，也就是**一两个像素**——
它整片压在投影源那一格自己头上，而下一步「影子不落在高物身上」把它擦得干干净净。
**真正落到可走地面上的，永远是这条斜坡衰减到尾巴的那一截。**
一栋房子（`hgt` 沿列累加到 2.5 格封顶、L = 40 逻辑像素）落到南邻那一格时
alpha 只剩 0.17，再往南一格只剩 0.05。`a: 0.30` 这个参数**从来没有兑现过**。

第二个后果更彻底：**矮东西一点影子都没有**。树 `TALL 0.75` → L = 12 逻辑像素 < 一格 16，
整条影子都在自己格子里，被擦光。六堆平原 164 棵树、内埔庄 125 棵，
在草地上一片影子都不投——而「山脚下的草是全亮的」正是第零节记的最刺眼那一条。

### 3.4 取舍（**动手前先写下来**）

**① 斜坡的锚点从「投影源」改到「影子钻出投影体的那一点」。** 这是这一轮唯一真正的改动。
沿光向步进，找到影子第一次离开高物的距离 `f0`，斜坡从 `f0` 起算、在 `f0 + L` 处归零。

- 为什么不是「把 `a` 调大」：`a` 是 `ambience.js` 里按场景分的三档（室外 / 洞窟 / 石城），
  **那个档是别人的文件，这一轮不碰**；而且调大只会把「峰值压在自己头上」这件事等比放大，
  地面拿到的仍然是尾巴。**参数没错，是几何错了**——改几何之后 `a: 0.30`
  的意思才第一次变成「墙脚压暗 30%」。
- 为什么不是「不擦高物格」：那样屋脊会把影子投在自家屋顶上，
  洞窟里整片岩壁会糊成一团黑。擦是对的，错的是斜坡跟着源格起算。
- 代价：每个投影源多一次沿光向的步进搜索（2 px 一步、最多 6 格）。
  **烘焙期跑一次**，逐帧仍然是两次 `drawImage`，运行时零成本。

**② `L` 的含义从「影子总长」改成「离开投影体之后还拖多远」，并给它一个下限 `MINL`（10 逻辑像素）。**
两个含义在整墙那一档差不多（墙的出口就在自己脚下），差别全在矮物件上。
改了锚点之后，树（0.75 → 12 px）自己就够了——那 12 px 现在整段落在邻格上；
**`MINL` 只管更矮的那一档**（`hgt < 0.625`：木箱 6.4、碎石 4.8、床 4 逻辑像素），
它们连出口都跨不过去，得有个下限才在右下角留得下一小片。
`cap`（单位是格）的语义不变，仍然是「这张图的影子最长几格」，
所以影子的**总跨度**几乎没变（房子 8+40+16 ＝ 64 px，改前是 40+16 ＝ 56 px），
变的只是那 0.30 的峰值到底落在地上，还是落在房子自己头上。

- 为什么不是「把 `SUN.len` 从 16 提到 20+」：那会把**所有**影子等比拉长，
  室内石城 `cap 1.4` 会变成 1.75 格，走廊全被影子填满；
  而问题只出在「短到跨不出一格」的那一档，该修的是下限不是斜率。

**③ 衰减指数 1.25 → 1.45。** 峰值真的落在地面上之后，1.25 会让整条影子偏浓、
拖出一条均匀的暗带；1.45 让它「墙脚实、一格半之内化掉」，接近午后斜射的读法。
这个数没有量化依据，是看画面定的，写在这里是为了让下一个人知道它可以调。

**④ `TALL` 补五张漏登记的物件瓦片**：`altar` 祭坛 / `seal_stone` 封门石
（这两张在壇下三层 `altar_1..3` 里是 `solid`，却不在投影表里——
和 `docs/交接.md` 第 3 节 ③ 那个 `stone_wall` 漏登记是同一类）、
以及已经有美术但还没进地图的 `sarcophagus` / `stove` / `idol`（先登记，免得下一张图用到时又漏）。
`water` **仍然留 0**：它是洞不是体积，给它高度池塘会往岸上投影。

**⑤ 明确不做的两件**（都不在这一轮的文件归属里）：
主角与 NPC 的落影仍然是 `npc.js` 里那个 `alpha 0.22` 的**居中**椭圆（第二节 2.3 计划过要
朝光向偏移、拉长、加外晕），粒子景深也还没接上 `par`——两样都要动 `npc.js` / `ambience.js`，
这一轮的文件归属只有 `lightmap.js`。写在这里给下一个人接。

---

## 四、实测结果（改后）

同一段探针代码、同一组格子、同一个镜头位置，改前改后各跑一次
（改前的那一轮是把 `lightmap.js` 临时换回 HEAD 的版本重量的，不是拿上一节的旧读数凑）。
**这一轮唯一改动的代码文件是 `src/field/lightmap.js`。**

### 4.1 建筑北侧与南侧的明度差（渲染帧上量，0–255）

| 地图 | 关 | 开（改前） | 开（**改后**） |
|---|---|---|---|
| 内埔庄 `village`（`26,24`） | 7.3 | 14.3 | **22.7** |
| 六堆平原 `overworld`（`10,11`） | 0.4 | 9.9 | **15.5** |
| 隘寮石城 `fort_ailiao`（`9,14`） | 0.8 | 7.3 | **13.2** |

**方向光真正贡献的那一段**（开减关）：7.0 → **15.4** / 9.5 → **15.1** / 6.5 → **12.4**，三张图都翻倍上下。

### 4.2 全图可走地面的平均压暗（直接量阴影贴图，0–255）

| 地图 | 改前 | 改后 |
|---|---|---|
| 内埔庄 | 7.2 | **16.5** |
| 六堆平原 | 8.4 | **14.3** |
| 隘寮石城 | 13.3 | **32.7** |
| 罗经圈外围 `cave_1`（顺手对照） | 16.2 | **37.4** |

### 4.3 各类投影源在**正南那一格**留下的压暗量（量贴图，0–255）

| 投影源 | 改前 | 改后 |
|---|---|---|
| 房子墙脚 `wall_base`（内埔庄） | 43.5 | **70.9** |
| 树 `tree`（内埔庄 / 六堆平原） | 28.9 / 30.5 | **53.0 / 50.6** |
| 林 `forest`（六堆平原） | 39.6 | **54.4** |
| 山 `mountain`（六堆平原） | 60.8 | **73.8** |
| 石墙 `stone_wall`（隘寮石城） | 46.5 | **74.6** |
| 廊柱 `pillar`（隘寮石城） | 32.5 | **50.3** |
| 木箱 `crate`（隘寮石城） | 22.0 | **57.4** |
| 床 `bed`（隘寮石城） | 17.7 | **46.3** |
| 岩壁 `cave_wall`（罗经圈外围） | 53.7 | **82.8** |

矮物件（树 / 木箱 / 床）涨得最多，正是 3.3 说的「L < 一格就整条被擦光」那一档。

### 4.4 没有把画面压黑（这是提强度必须一起量的那一条）

量的是可走地面在**渲染帧**上的明度分位数，取最暗的两张图：

| 地图 | p01（关→开） | p50（关→开） | 明度 < 16 的比例 |
|---|---|---|---|
| 罗经圈深处 `cave_2` | 20.6 → 14.8 | 36.4 → 28.6 | 0% → **1.9%** |
| 万金古塚 `tomb_wanjin` | 21.0 → 16.6 | 48.9 → 42.9 | 0% → **0.9%** |
| 隘寮石城 | 31.3 → 25.9 | 76.9 → 64.8 | 0% → **0%** |
| 内埔庄 | 40.2 → 35.7 | 95.8 → 91.3 | 0% → **0%** |

最暗的那张图也只有 1.9% 的地面掉到 16 以下，**没有一处压到纯黑**。
洞窟本来就该暗（`cave_2` 的设定是「只靠磷光石照明」），而主角脚下有 `vig` 的亮区托着。

### 4.5 成本

**运行时：零。** 逐帧仍然是 `drawLit` / `drawShade` 各一次 `drawImage`，
贴图张数与尺寸一个字没变，变的只是贴图的内容。

**烘焙期**（换地图时跑一次，被 `fadeTo` 的淡出盖住）：十七张图一次烘完
**226 ms → 236 ms（+4%）**，多出来的就是那个沿光向的出口搜索。
最贵的单张是六堆平原（640×512 的贴图）**35~47 ms**——单张的前后差被抖动淹掉了，
所以只报总数。这一档的大头是画布分配与那次降采样模糊，不是拖影的 `fillRect`。

**防闪**：两张贴图与时间完全无关，逐帧画的是同一张图。这一轮没有引入任何随时间变化的量。

---

## 五、评估过但**决定不做**的

1. **高处物件的 y 位移**（原第 4 位）。把屋顶 / 崖顶那一格往上挪几个像素能立刻做出高差，
   但**它和网格碰撞是两套坐标**：画面上挪了、能不能走没挪，玩家会撞到看起来是空地的地方。
   `CLAUDE.md` 禁止条「像素级碰撞（一律网格制）」堵死的正是这条路的出口。
   要做高差得先有高度轴，那是 `docs/HD2D方案.md` 阶段 3 的事。
2. **把两张贴图合成一张、用 `overlay` / `soft-light`。** 省一次 `drawImage`，
   但那两个合成模式这个项目一次都没验过；万一在某个浏览器上退化成 `source-over`，
   就是把整张贴图**盖在画面上**——灾难性失败，不是降级。多一次 `drawImage` 买这个保险。
3. **逐帧算光照 / 让影子跟着主角走。** 主角是移动光源这条听起来很对，但
   `docs/HD2D方案.md` 第五节第 6 条实测过：逐像素处理会把画布降级成软件光栅且不恢复。
   而且方向光一旦跟着人转，`tiles.js` 那整套「按左上来光画死」的明暗序就全穿帮了。
   **全场只认一个光向**是 HD-2D 的做法，也是这个项目已经在守的规矩。
4. **地面的接触影（AO）挪进这张贴图。** `terrainBake.js` 的 `shadowTile` 已经在做，
   而且它是「一格一张缓存图」，做接触影正合适（只看八邻居）。两边分工是清楚的：
   **跨格的太阳影归贴图，贴着物件根部的那一圈归瓦片**，方向一致（都朝右下），叠起来正好是
   「实的接触影 → 化开的太阳影」。不要把它搬过来，搬过来就要重烘所有瓦片。

---

## 六、结论

走地图这半的 HD-2D 是**三样东西**：受光棱（加法）、立面暗部（转折）、长投影（体积）。
前两样第一轮就立住了；长投影第一轮**写对了结构、错了一个锚点**，
于是三样里最能说明「这里有个体积」的那一样，在画面上等于不存在。

这一轮只改了那个锚点（外加一个落地下限、五张漏登记的物件瓦片），
三张验收图上「建筑北侧与南侧的明度差」从 14.3 / 9.9 / 7.3 提到 **22.7 / 15.5 / 13.2**，
全图地面平均压暗从 7.2 / 8.4 / 13.3 提到 **16.5 / 14.3 / 32.7**，
而运行时一次 `drawImage` 都没多、烘焙期只多了 4%。
改前那组截图关掉再打开肉眼分不出来，改后是**一眼看得见房子和山在地上压出了影子**。

**教训跟这个项目已经记过的那条是同一条**：`a: 0.30` 写在参数表里、
代码也确实每一格都画了影子、测试全绿、控制台干净——
但**没有人量过它到底压暗了多少**。参数写着 30%，画面上拿到 3%。
渲染这一层，「代码看起来是对的」和「效果发生了」之间隔着一次实测。
