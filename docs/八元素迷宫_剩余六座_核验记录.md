# 八元素迷宫剩下六座：规格 + 对抗验证记录（2026-09-08）

> 派了一个 Workflow（12 个 agent：六座各一次「出规格」→「对抗验证」）。
> **六座全部判 `needs-work`，没有一座能直接照做。** 这是好事——问题是在动手前被挡下来的，
> 不是上线之后才发现。本档留着每一座的核心问题与修法，下一轮直接照着改，
> 不必重新跑一次调查（那一轮花了 279 万 tokens、619 次工具调用）。
>
> 完整原始记录（每条 blocking/silent 的全文，含行号）在
> `/tmp/claude-501/-Users-barry-2D-Game/141c6fcf-7193-42ca-bfa5-13188bac2fb2/scratchpad/dungeon_verify_full.json`
> ——那是本次会话的临时目录，**不保证还在**；这份文档摘的是要点，够用来动手。

## 六座的共同病灶（不是各自独立的巧合）

三种病反复出现，说明是「出规格」这一步系统性漏看的东西，不是运气：

1. **难度算错了参照系。** 六座里至少四座（木馬道、風口、空伙房、塭岸）把
   「進入等級」定在文档写的幕次（20 级上下），但实际接线是**从任何时候都进得去的
   overworld 挂一个 warp**，没有任何等级闸门——跟壇下/爐底/隘寮/古塚同一种接法。
   结果是「文档设计的高等级迷宫」用「随时能进」的方式接了线，玩家 1 级就能走进
   一个按 20 级配的遇敌表。**接线方式和难度设定必须对齐，二选一**：要么加 flag 闸
   门禁到某个剧情点之后，要么把遇敌数值降到「随时能进」那一档（参照现有六个
   随时进得去的区）。

2. **宝箱收益算错了量级。** 每一份规格给的箱子总值都是现有全部宝箱的数倍
   （木馬道一座就有 10415 金等值，现有全部地图宝箱现金加起来才 4430）。
   `tests/balance.js` 现有四条路线的曲线是装备驱动的（CLAUDE.md「Boss 难度曲线」），
   塞进去足量的高价值道具会直接冲垮那条曲线，而没有一份规格在写完之后跑过
   `tests/?balance` 去对表。

3. **`tests/balance.js` 结构上看不到饰品。** 六份 risk 清单里有好几份说
   「改完记得跑 balance 验证」，但 `tests/balance.js:108` 的装备表只有
   `{weapon, armor}` 两格，饰品恒为 null——塞进箱子里的戒指/鞋子完全不会反映在
   模拟数字上。**这是测试基础设施的缺口，不是某一份规格的错**，六座迷宫谁先接线
   谁先撞上，建议在正式接第一座之前先把这个补上（大约 20 行：LOADOUTS 加
   accessory 字段，scene() 传进 equipment）。

## 逐座状态

| 迷宫 | 元素 | 生成器 | 预估修复 | 卡点数（致命/静默）|
|---|---|---|---|---|
| ~~燈（鵝鑾鼻燈塔）~~ | 光 | temple 复用 | **已接线，单层版** | 8 / 8 |
| ~~風口~~ | 風 | cave 变体 | **已接线** | 2 / 7 |
| 空伙房 | 暗 | castle 复用 | 6h | 7 / 9 |
| 木馬道 | 木 | mine 换皮 | 5h | 5 / 8 |
| ~~出火地縫~~ | 火 | cave 复用 | **已接线** | 6 / 9 |
| 塭岸 | 水 | mine 复用 | 6h | 4 / 7 |

### ~~燈（光，鵝鑾鼻燈塔）~~ ← 已接线（2026-09-08），单层版

**没有照抄规格三层的方案**——对抗验证抓到三层其实是同一张平面图（temple 生成器
几何写死，seed 只换门的方位，逐格比对 lamp_1/2/3 差异只有 1.6%–1.9%），
而且和仓库里已经躺着的 gen_temple_1.json 也几乎是同一张、跟主线第八幕「地心 · 環廟」
排的又是同一个原型。做三层等于预支環廟的平面图，还三层长得一样。

改成只做**一层**（`lamp_1`，规格自己的 cheapestPlayableVersion 就是这么建议的）：
- 上下楼梯方向修正：`<` 改回 stairs_down、`>` 改 stairs_up
  （这座是往上走的迷宫，规格原本给反了——`dungeon_kit.py` 定义 `<`=上行 `>`=下行）
- 塔心那道 `>` 不接 warp，当「还没盖上去的塔顶」，跟 altar_3/furnace_2 悬空楼梯同一个先例
- 5 个箱子内容按规格改（生成器给的是固定模板，规格列的东西一件都没进 diff，
  还会白送一件 tier 3 防具，规格自己写的「不给装备阶梯」当场失效——已经堵上）
- `src/field/minimap.js` 补 sand 的小地图配色（`#9c8f6a`→`#d8c48c`），
  跟 flagstone 的对比从 Δlum 19 拉到 72.8——光带机制在小地图上终于看得出来
  （这个改动是全局的，其余用到 sand 的地图顺带受益，不会降低任何地方的对比）
- 补了 `tests/balance.js` 一直漏掉的五个区（altar/altar_deep/furnace/furnace_deep/lighthouse）
  的等级取样表，之前全部落到兜底 `[3,6,9]`，量出来的数字答的是另一道题

验收：从村子沿 warp 图走得到；实机渲染背景是 `shrine` 不是 `default`；
`tests/?balance` 的 lighthouse 区量在真实等级（5–8 级）上，胜率/掉血/回合数都正常；
playtest 全程通关。96/96 通过。

### 燈（光，鵝鑾鼻燈塔）原始规格与验证记录（存档，接线细节见上）—— 3h，六座里最便宜

- **宝箱内容全错**：三条命令行只生成 temple 的固定模板（ether/bell/gold/hipotion/silk_robe），
  规格列的 15 件道具一件都没写进 diff。silk_robe 是 tier 3 防具，会撞进
  `tests/balance.js` 现有 herbwife/talisman 的装备表，制造装备量级混乱。
- **上下楼梯用反了**：`<`=上行 `>`=下行（`dungeon_kit.py:42-43`），
  文档写死这座是「全作唯一往上走的迷宫」，规格的三条命令行给反了。
- **小地图上光带和阴影分不出来**：flagstone `#8a7a5e` vs sand `#9c8f6a`，
  亮度差只有 19/255（CLAUDE.md 判准：10.9 是分不清、37.4 是修好）。
  这座的招牌机制「光帶不遇敵」，玩家在小地图上完全看不出哪条是光带。
- **`tests/?balance` 没有这两个区的等级取样**，会落到兜底 `[3,6,9]`，
  量出来的数字答的是另一道题。

### ~~風口~~ ← 已接线（2026-09-08）

按 CLAUDE.md「ZONE_BG/encounters.json/config.json 三件绑成一次改动」的建议，
避开了規格自己留的顺序陷阱（先加 encounters.json 会让 `每个遇敌区都有对应的
战斗背景` 那条测试当场红，不是静默）。

三张图（風吹沙 → 響石窟東風／西風）用同一颗 seed 生成響石窟那两张，
只有一行 legend 不同——`S`（沙岸）東風态填成 `rubble solid`（積沙埋了水潭）、
西風态留 `sand`（沙被风扫开）。切换点在 `(2,1)` 那个洞：東風踩上去落到西風的
`(2,2)`，西風踩上去落回東風的 `(2,2)`——纯 warp，一行引擎代码没动。
两个状态的可走格数**逐格 BFS 验过**：東風 433 格、西風 453 格，和规格给的数字
分毫不差；埋潟湖之后有一个宝箱够不着了，直接删掉（规格漏了这步，会在
可达性测试里当场报错）。

宝箱内容全部按规格重写（生成器给的是 cave 固定模板，规格列的东西一件都没进
diff）；`swift_boots` 换成了 `power_band`——因为「燈」那座已经放过 swift_boots，
两座迷宫撞同一件东西不好看，power_band 同样是「商店已经在卖，不是新台阶」。

补了「你能改風向」在画面上的证据：`ambience.js` 给三张图配了粒子横吹，
東風 `vx:[14,26]`、西風 `vx:[-26,-14]`——掏完洞回来粒子立刻反着吹，
不用一个字说明「风向变了」。另外在两态共享的墙角开了一小片風影
（不遇敌的沙地，字符另开 `s`，没有借用东风态已经变成实心的 `S`）。

验收：从村子走得到；三张图渲染背景分别是 cave/deep/deep；两态可走格数
432/453（实机 BFS 量的）；在游戏里真的踩开关走了一趟，`windgap_2(2,1)`→
`windgap_3(2,2)`、`windgap_3(2,1)`→`windgap_2(2,2)`，两个方向都对；
粒子方向反着吹验过；96/96 通过，playtest 全程通关。

### 空伙房（暗）—— 6h，问题最多

- **规格自称已经做完①～⑤步验证过，实际上④⑤两步一步都没做**——生成出来的
  箱子内容跟规格列的完全对不上。这一条比其他任何一座都严重：不是遗漏，是虚报进度。
- **真正的死档风险**：唯一通往 overworld 的格子是全图仅有的存档可达点，
  而引擎允许随处存档——有一种走法会在这一格存档后卡死（周围三面 solid）。
- loot 两条理由和 `shop.json` 现有商品直接矛盾（写成「稀有寄卖」的两件其实是商店常驻货）。
- `pickVariant` 的 `if` 数组是**与**不是**计数**，规格设计的「读到第几个坛」变体
  逻辑在引擎里跑不出来。
- 一个箱子（`urn_1`）规格说「进门拐两步」，实测 BFS 是 16 步。

### 木馬道（木）—— 5h，详情见上一条消息，不重复

核心：难度对照组选错（比全作最深两区还硬）、第一层塞了两只不该在第一层出现的
重甲/高血怪、箱子收益顶掉全作宝箱现金总和的两倍、和设计文档「t8 精金」的规定
冲突却没有二选一、`#`→tree 会让墙脚影子消失且六成树退回自带草底（跟 CLAUDE.md
记的 stone_wall 那一课同一种坑，只是这次是「投影值为 0 的 falsy」而不是「漏登记」）。

### ~~出火地縫~~ ← 已接线（2026-09-08）

**做了一个规格与验证都没提出的决定：整座迷宫挂在打完乌火之后。**
对抗验证量出规格给的难度在等级 3 就已经越过全作现有天花板（`chuhuo_deep`
30% 胜率、四个人倒三个），而入口离村口只有 15 步、warp 本身又挂不了 flag——
规格把「不设门槛」讲成设计选择，其实是引擎当时唯一做得到的事。

解法没有碰引擎：在 overworld 入口唯一的必经格放一个 `unless: bossDefeated`
的 NPC（跟 `village` 的 `gate_guard`、`cave_3` 的 `watcher_top` 同一个机制），
挡住的是通道不是 warp。`watcher_top` 打完 Boss 后原话就是「我该转去恒春了」——
出火在恒春，这两句台词本来就是同一条线，现在接上了。

被这个决定连带解开的两件事：
- 验证量出的「等级 3 就已经越线」不再是问题——玩家真正抵达这里时通常已经
  10 级上下（`CLAUDE.md`「Boss 难度曲线」纯商店路线 10–12 级才打得过乌火）。
  只把第一层的两组地牛拿掉、其余照原规格，量出来是 100% 胜率、掉血 6–11%，
  健康的「打完主线的奖励关」难度，不是二周目 Boss。
- 规格设计的两处「看火的」挡路 NPC**没有做**——对抗验证量出它们要挡
  24 格＋4 个箱子＋下行梯，是全作现有最大先例（`village/gate_guard` 挡 2 格）
  的 12 倍，而且从没在浏览器里跑过。现在整座迷宫内部畅通，跟壇下/爐底/
  燈/風口四座已接线的迷宫一致——没有一座在内部设关卡，只在入口设。

其余照单全收的修正：
- warp 的 facing 生成器写死成 `down`，只有一条碰巧对，另外两条改成
  `up`/`left`（背对来路）
- `w`→`rubble` 塌方堵缝、`S`→`sand` 去掉遇敌（灰白土，喘息点）
- chuhuo_1_6 从发金币改成发道具时**删掉了 gold 键**——这是规格自己漏的一处，
  不删的话箱子会金币道具一起发
- 手加第 6 个箱（补运汤）在 `(2,4)`，紧挨着一段没接线的下行梯——跟 `altar_3`
  同一个先例：「走不下去，箱子摆在旁边看」直接写进了叙事文本
- `ambience.js` 补了这座应得的暖橙色调（抄 `docs/八元素迷宫.md` §1.6 那张表），
  不再是全作唯一没有气氛层的火系迷宫
- `tests/balance.js` 补了 `chuhuo`/`chuhuo_deep` 的等级取样（挂 8–12 级，
  对应「刚通完主线」），以及漏掉的 `windgap`/`windgap_deep`（上一座忘记加）

验收：从村子沿传送图走得到；未打 Boss 前入口被挡、打完后放行（实机验证过
两种状态）；两张图渲染背景 cave/deep；`tests/?balance` 在 8–12 级量出
100% 胜率、掉血个位数；96/96 通过，playtest 全程通关（含打完 Boss 那一步，
等于顺带验了闸门真的会开）。

### 塭岸（水）—— 6h

- `--chests 0` 生成的两张图（潮态切换用）事件里没有箱子 id，
  跟规格给的 loot 表对不上号（loot 表要求的字段测试里是必填的）。
- `config.json` 只提了三张图里的一张，另外两张不接线就会在可达性测试里报「传送到不存在的地图」。
- 满潮退潮切换那张图有一条「满潮捷径」在生成的地形里其实是死路——
  两侧本来就有等长的常开通道，捷径没有捷径到的意义。
- 门禁问题最严重的一座：入口挂在第一幕就走得到的地方，而遇敌强度是全作最高
  （`pond_high` 238.5 vs 现有最高 `furnace_deep` 170.3）。
- steps 区间算出来的经验/金钱水龙头会把 Boss 曲线冲垮，规格完全没提这件事。

## 建议的下一步

**不要直接拿这三份规格去接线。** 燈、風口、出火地縫都做完了（2026-09-08）。

剩下三座：木馬道(5h) → 空伙房/塭岸(6h)。一次修一座、修完立刻接线验收
（照 CLAUDE.md 的四步：lint+测试 → 进游戏看到 → 发布 → 线上再确认），
不要三座一次改完再验——那样出错了不知道是哪一座的锅。

「出火地縫」那一份多一条经验：**规格算错的「不设门槛」有时不是数值问题，
是接线方式问题。** 硬调数值降难度只是让它变得平庸；真正对的做法是先问
「这座该在游戏进度的哪个点出现」，答案往往已经藏在别的地方——`watcher_top`
那句「我该转去恒春了」等在那里，接上就是。

「燈」那一份的经验值得抄：**规格给的方案不一定是该照做的方案**——
它给三层，对抗验证发现三层是同一张图，改成只做一层反而更好；
它的宝箱内容生成器根本不会生成，得手改；上下楼梯方向、悬空楼梯的处理、
小地图配色这类「细节」往往比「接不接线」本身更花时间。
